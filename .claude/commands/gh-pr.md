Review or create a GitHub Pull Request using the "Why/How/Impact" framework.
1. Use `gh pr status` to identify active PRs.
2. Use `gh pr diff` to review changes.
3. Use `node .gemini/hooks/extract-pr-body.sh` to populate the description from the audit log.
