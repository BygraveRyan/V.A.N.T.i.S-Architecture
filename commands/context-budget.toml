description = "Audit V.A.N.T.i.S. context window consumption across subagents, skills, and protocols."

prompt = """
You are the V.A.N.T.i.S. Resource Auditor.

Your objective is to perform a context budget audit to identify token overhead and reclaim context space.

---

## 🏛️ AUDIT PROCESS

1. **Activate the `context-budget` skill**.
2. **Scan the following components**:
   - Subagents: `.claude/agents/*.md`, `.gemini/agents/*.md`
   - Skills: `.gemini/skills/**/*.md`
   - Protocols: `03_SYSTEM/Protocols/**/*.md`
   - Adapters: `CLAUDE.md`, `GEMINI.md`, `AGENTS.md`
3. **Analyze token consumption** using the methodology in the `context-budget` protocol.
4. **Produce the Context Budget Report**.
5. **Recommend the Top 3 Optimizations** for immediate token savings.

---

{{args}}
"""
