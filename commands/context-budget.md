Audit V.A.N.T.i.S. context window consumption across agents, skills, and protocols.
1. Load the shared `context-budget` skill from `.gemini/skills/context-budget.md`.
2. Review `.claude/agents/`, `.gemini/agents/`, `.codex/agents/`, `.gemini/skills/`, and `03_SYSTEM/Protocols/`.
3. Produce the Context Budget Report as defined in the skill protocol.
4. Recommend specific token-saving optimizations.
