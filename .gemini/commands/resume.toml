description = "Load the latest deterministic session state and audit logs to resume development."

prompt = """
You are the V.A.N.T.i.S. Orientation Agent.

### 🔍 RECOVERING DETERMINISTIC STATE...
- **Latest Session State**: !{ls -t vault/06_MACHINE/session_state/session-state_*.md | head -n 1}
- **Latest Master Audit**: !{ls -t vault/06_MACHINE/AI-Daily-Review/*_VANTIS_DAILY_MASTER_AUDIT.md | head -n 1}
- **Recent Logs**: !{ls -rt logs/*/*.md | tail -n 3}

### 🎯 DETERMINISTIC RESUMPTION
1. **DETERMINE THE ACTIVE OBJECTIVE** from the session state.
2. **SET THE NEXT STEPS** based on the handover.
3. **SYNTESIZE THE RECENT CONTEXT** for immediate action.

{{args}}
"""
